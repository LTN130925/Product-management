include ./product-item.pug

mixin product-grid(products)
  .row
    each item in products
      +product-item(item)

mixin blog-grid(blogs)
  .row 
    each item in blogs 
      +blogs-item(item)
      